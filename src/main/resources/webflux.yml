feedbackconst:
  webFluxOrquestador:
    urlBase: ${URL_ORQUESTADOR}
    endpoints:
      entrevistaMuestraId: /v1/entrevistador/public/entrevista_muestra_id?perfil=
  webFluxFeedbackPregunta:
    urlBase: ${URL_PREPARADOR_ENTREVISTA}
    endpoints:
      obtenerFeedbackPreview: /feedback/preguntas
  webFluxNotificaciones:
    urlBase: ${URL_NOTIFICACIONES}
    endpoints:
      enviarEvento: /v1/eventos/enviar/
  limitPreguntas: 3